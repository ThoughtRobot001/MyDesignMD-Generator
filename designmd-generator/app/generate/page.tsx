import LandingApp from "../../components/landing/App";

/** Renders the integrated mydesign.md frontend on the generate route. */
export default function GeneratePage(): JSX.Element {
  return <LandingApp />;
}
